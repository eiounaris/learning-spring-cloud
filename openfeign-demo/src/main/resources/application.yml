server:
  port: 8083

spring:
  application:
    name: openfeign-demo

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
